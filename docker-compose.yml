services:
  # Next.js-Website als Standalone-Server, direkt auf Port 80.
  # Ohne Proxy, ohne HTTPS — erreichbar unter http://<deine-VPS-IP>.
  web:
    build: .
    container_name: website
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
